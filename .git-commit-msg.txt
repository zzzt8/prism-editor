feat(image-ops): Canvas 2D apply-mask implementation with performance optimization

Breaking changes:
- Worker canvasPool reuse logic + Comlink ImageData cloning

Key additions:
- Canvas 2D compositing for alpha/brightness/luminance masks
- CanvasPool with LRU eviction and TTL support
- Performance benchmark suite (4K <50ms, 8K <200ms targets)
- Worker runner with main-thread fallback
- Preview strategy (eager vs lazy data URL)
- Comlink transfer handler for ImageData

Also tracked:
- .cursor/ folder with OpenSpec AI skills (opsx-*)
- openspec/ changes for apply-mask-canvas-optimization
- Archive of completed image-synthesis-performance change
