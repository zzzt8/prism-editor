---
description: Prism architecture guardrails
alwaysApply: true
---

# Prism Architecture Guardrails

每次任务开始前，先读取：
- `docs/architecture/PRISM_TARGET_ARCHITECTURE.md`
- `docs/architecture/PRISM_ARCHITECTURE_GUARDRAILS.md`
- `docs/architecture/PRISM_MIGRATION_ROADMAP.md`

强制规则：
- 开始任务前声明当前 migration phase 和 OpenSpec change ID；无 phase/change ID 不得开始跨层修改。
- 实现任务不得修改目标架构、架构护栏、迁移路线图和本 Rule；架构文档变更必须是独立决策任务，需人工批准。
- 实现与架构冲突时必须停止并报告；不得通过修改护栏适配实现。
- 不得新增 Mall 用户、SKU、购物车、订单或工厂账号模型。
- 不得恢复 `user-app`、`PublishedWorkflow` 或 Prism 用户登录系统。
- 不得把 Mall 的 `userId`、`skuId`、`orderId` 写进公共 `ProductTemplate`。
- 不允许 Composer 与 Browser Runtime 长期保留两套独立渲染逻辑。
- 浏览器预览和 Node 生产必须共享同一套 `DesignState` 与参数语义。
- 生产端必须使用原始素材，不得把前端压缩预览图作为生产源。
- Flow 必须通过稳定 `flowKey` 显式选择。
- 输出必须通过 `explicitOutputs` 显式声明。
- 禁止使用 `findFirst` 或对象遍历顺序决定生产流程与最终输出。
- 新增普通品类只能通过模板和 Flow 配置，不得要求修改 Mall 前端。
- 每次只推进一个迁移阶段，不得顺手重构无关模块。
- 任何新增 package、数据库表或公开 API 都必须在当前 OpenSpec 范围内。
- 平台依赖必须隔离：Browser Runtime 不得引入 Node-only 依赖，Production Runtime 不得依赖浏览器 UI。
- 公共协议必须 JSON 可序列化，不得携带 Blob、File、Canvas、ImageBitmap、DOM 节点、函数、Store 或 blob URL。
