{
  "name": "@prism/image-ops",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@prism/shared-types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@vitest/browser": "^1.6.0",
    "playwright": "^1.44.0"
  }
}
